# Novels community database

The live schema is managed in Supabase project `novels-community` (`efftrxqdsrmyuaubjumh`).

All exposed public tables have Row Level Security enabled. This directory is reserved for checked-in migrations as the beta evolves.
